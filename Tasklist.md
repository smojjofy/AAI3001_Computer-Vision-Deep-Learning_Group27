# Repository Tasklist

## Current objective

- [ ] Establish the single-camera pipeline: stream handler → frame processor → inference → splat constructor → temporal cache → Three.js viewer.
- [ ] Keep module interfaces synchronized through `shared/schemas/`.
- [ ] Use recorded fixtures so modules can be developed independently.
- [ ] Add integration tests once the first vertical slice exists.

## Future scope

- [ ] Add multi-camera synchronization and calibration.
- [ ] Add pose estimation and multi-angle depth/segmentation.
- [ ] Evaluate Unreal integration separately from the tracked team project.
