# Configuration Tasklist

- [ ] Define default runtime, camera, model, and rendering configuration.
- [ ] Provide example configuration files without credentials or private camera URLs.
- [ ] Document units, coordinate axes, depth conventions, and update rates.
- [ ] Validate configuration before starting the pipeline.
