# Modules Tasklist

- [ ] Keep each processing stage independently runnable.
- [ ] Require every module to document its input and output contract.
- [ ] Keep shared schemas in `shared/schemas/` rather than duplicating packet definitions.
