# Three.js Viewer Tasklist

Input: `ReconstructionState` from Temporal Cache and a static environment asset.

Output: interactive Three.js view with Gaussian reconstruction, alpha compositing, relative-depth visualization, and diagnostics.

- [ ] Render full and delta reconstruction updates.
- [ ] Load a static background or 3D environment.
- [ ] Keep the initial viewer camera fixed or close to capture view.
- [ ] Add GUI controls for confidence, depth, alpha, and latency.
