# Depth Tasklist

Input: normalized `ProcessedFrame`.

Output: relative depth map and per-pixel depth confidence aligned to the input dimensions.

- [ ] Select and integrate a pretrained monocular-depth model.
- [ ] Define normalization, invalid-depth, and confidence behavior.
- [ ] Document that output is not metric depth in the single-camera MVP.
